<?xml version="1.0" encoding="utf-8"?>
<!--
  Background for the Add-Fillup 1x1 widget (#121). Rounded
  rectangle using the system surface tones so the widget reads
  as a native Material You tile on Android 12+. The fallback
  for older releases uses a fixed dark color so the widget
  still looks intentional.
-->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
       android:shape="rectangle">
    <solid android:color="?android:attr/colorBackground" />
    <corners android:radius="20dp" />
    <stroke android:width="1dp" android:color="#26FFFFFF" />
</shape>
